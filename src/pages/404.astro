---
/** Rota 404. Toda a apresentação é do tema ativo. */
import NotFound from '@theme/NotFound.astro';
import { getSiteConfig } from '../lib/site';

const site = await getSiteConfig();
---

<NotFound site={site} />
