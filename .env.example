# ---------------------------------------------------------------
# AETHER Blog — site público estático (Vite + Supabase)
# Não há backend: estas variáveis são embutidas no bundle no build.
# Use SOMENTE a anon key, e mantenha a escrita fechada no Supabase
# (leitura pública via RLS, escrita apenas com service_role no Studio).
#
# Local:  copie este arquivo para .env
# Vercel: Project Settings > Environment Variables
# ---------------------------------------------------------------
VITE_SUPABASE_URL=https://nxutdbhcedjcdfvsbrzt.supabase.co
VITE_SUPABASE_ANON_KEY=
